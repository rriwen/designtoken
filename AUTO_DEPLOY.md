# Vercel 自动部署配置指南

## 🚀 自动部署已准备就绪

项目已配置好自动部署所需的所有文件。只需在 Vercel Dashboard 中完成一次设置，之后每次推送代码到 GitHub 都会自动部署。

---

## 📋 设置步骤

### 第一步：在 Vercel 中导入项目

1. **访问 Vercel Dashboard**
   - 打开 https://vercel.com
   - 使用 GitHub 账号登录

2. **导入 GitHub 仓库**
   - 点击 "Add New..." → "Project"
   - 选择 `rriwen/designtoken` 仓库
   - 点击 "Import"

3. **配置项目设置**
   - Framework Preset: **Vite** (自动检测)
   - Root Directory: `./` (默认)
   - Build Command: `npm run build` (已配置)
   - Output Directory: `dist` (已配置)
   - Install Command: `npm install` (已配置)

4. **启用自动部署**
   - 在 "Git" 部分，确保：
     - ✅ "Production Branch" 设置为 `main`
     - ✅ "Automatic deployments from Git" 已启用（默认启用）

5. **部署**
   - 点击 "Deploy" 按钮
   - 等待首次部署完成

---

## ✅ 自动部署工作原理

### 触发条件

Vercel 会在以下情况自动部署：

1. **推送到 main 分支**
   ```bash
   git push origin main
   ```
   → 自动触发生产环境部署

2. **创建 Pull Request**
   - 创建 PR 时自动创建预览部署
   - PR 合并到 main 时触发生产部署

3. **手动触发**
   - 在 Vercel Dashboard 中点击 "Redeploy"

### 部署流程

```
GitHub Push → Vercel Webhook → 自动构建 → 自动部署 → 完成 ✅
```

---

## 🔧 验证自动部署

### 测试自动部署

1. **修改代码**
   ```bash
   # 修改任意文件
   echo "// 测试自动部署" >> src/app/App.tsx
   ```

2. **提交并推送**
   ```bash
   git add .
   git commit -m "test: 测试自动部署"
   git push origin main
   ```

3. **查看部署状态**
   - 访问 Vercel Dashboard
   - 在 "Deployments" 标签中查看新的部署
   - 应该会看到 "Building..." → "Ready" 的状态变化

4. **验证部署**
   - 部署完成后，访问你的网站 URL
   - 确认更改已生效

---

## 📊 部署状态监控

### 在 Vercel Dashboard 中

1. **查看部署历史**
   - 进入项目 → "Deployments" 标签
   - 查看所有部署记录

2. **查看构建日志**
   - 点击任意部署
   - 查看 "Build Logs" 了解构建详情

3. **查看部署状态**
   - ✅ Ready: 部署成功
   - ⏳ Building: 正在构建
   - ❌ Error: 部署失败（查看日志）

---

## 🔔 通知设置

### 配置部署通知

1. **进入项目设置**
   - 项目 → Settings → Notifications

2. **启用通知**
   - ✅ Email notifications for deployments
   - ✅ Slack notifications (可选)
   - ✅ GitHub commit status (自动)

---

## 🛠️ 故障排除

### 问题：自动部署未触发

**解决方案：**
1. 检查 GitHub 仓库设置
   - Settings → Webhooks
   - 确认 Vercel webhook 存在且正常

2. 检查 Vercel 项目设置
   - Settings → Git
   - 确认 "Production Branch" 为 `main`
   - 确认 "Automatic deployments" 已启用

3. 检查构建日志
   - 查看是否有构建错误
   - 确认 `npm run build` 在本地可以成功运行

### 问题：部署失败

**解决方案：**
1. **查看构建日志**
   - 在 Vercel Dashboard 中查看详细错误信息

2. **本地测试构建**
   ```bash
   npm run build
   ```
   - 如果本地构建失败，修复问题后再推送

3. **检查依赖**
   - 确保所有依赖都在 `package.json` 中
   - 运行 `npm install` 确保依赖正确

4. **检查 Node.js 版本**
   - Vercel 默认使用 Node.js 18.x
   - 如需指定版本，在 `package.json` 中添加：
     ```json
     "engines": {
       "node": "18.x"
     }
     ```

---

## 📝 当前配置状态

### ✅ 已配置的文件

- ✅ `vercel.json` - Vercel 配置文件
  - 构建命令：`npm run build`
  - 输出目录：`dist`
  - SPA 路由重写配置
  - 静态资源缓存配置

- ✅ `package.json` - 项目配置
  - 构建脚本已配置
  - 所有依赖已列出

- ✅ `.gitignore` - Git 忽略文件
  - 已排除 `node_modules` 和 `dist`

### 🎯 部署分支

- **生产环境**: `main` 分支
- **预览环境**: 所有 Pull Request

---

## 🚀 快速命令参考

```bash
# 本地构建测试
npm run build

# 提交更改
git add .
git commit -m "你的提交信息"

# 推送到 GitHub（自动触发部署）
git push origin main

# 查看部署状态
# 访问 Vercel Dashboard → Deployments
```

---

## 📚 相关文档

- [Vercel 官方文档](https://vercel.com/docs)
- [Git 集成指南](https://vercel.com/docs/concepts/git)
- [自动部署文档](https://vercel.com/docs/concepts/deployments/overview)

---

## ✨ 完成！

设置完成后，你的工作流程将是：

1. 修改代码
2. 提交到 GitHub
3. **自动部署** ✨（无需手动操作）
4. 访问更新的网站

**就是这么简单！** 🎉
