# OceanBase Design Token System

一个功能强大的设计令牌管理系统，用于管理和导出 OceanBase 设计系统的所有设计令牌。

## 功能特性

- 🎨 **完整的设计令牌管理**
  - 基础颜色 (Primitives)
  - 语义颜色 (Semantics)
  - 字体 (Typography)
  - 圆角 (Radius)
  - 阴影 (Shadow)
  - 间距 (Spacing)

- 📦 **多格式导出**
  - JSON 格式的 Design Token 文件
  - CSS 格式的全局样式文件
  - 支持 CSS 和 JS 两种代码语法格式

- 🔍 **强大的搜索功能**
  - 实时搜索所有设计令牌
  - 支持按名称、值、代码语法搜索

- 🌐 **多语言支持**
  - 支持中文、英文、日文字体配置

- 📋 **更新记录**
  - 完整的更新历史记录
  - 版本管理

## 技术栈

- **React 18** - UI 框架
- **TypeScript** - 类型安全
- **Vite** - 构建工具
- **Tailwind CSS** - 样式框架
- **Radix UI** - 无障碍组件库
- **JSZip** - 文件压缩

## 开发

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 预览生产构建
npm run preview
```

## 部署

### Vercel 部署

项目已配置 Vercel，可以直接部署：

1. 将代码推送到 GitHub
2. 在 [Vercel](https://vercel.com) 中导入项目
3. Vercel 会自动检测 Vite 配置并部署

### 手动部署

```bash
# 构建项目
npm run build

# 构建输出在 dist 目录
# 可以将 dist 目录部署到任何静态文件服务器
```

## 项目结构

```
src/
├── app/
│   ├── App.tsx              # 主应用组件
│   └── components/          # UI 组件
├── data/
│   ├── token/              # Design Token 数据文件
│   └── mockTokens.ts       # 模拟数据
└── styles/                 # 样式文件
```

## 导出文件说明

导出时会生成以下文件：

- `color-seed.json` - 基础颜色令牌
- `color-semantic.json` - 语义颜色令牌
- `font.json` - 字体令牌
- `radius.json` - 圆角令牌
- `space.json` - 间距令牌
- `shadow.json` - 阴影令牌
- `global-styles.tokens.json` - 全局样式令牌（JSON 格式）
- `global-styles.css` - 全局样式文件（CSS 格式，可直接在 WEB 应用中使用）

## 使用全局样式文件

在 WEB 应用中使用 `global-styles.css`：

```css
@import './global-styles.css';
```

或直接在 HTML 中引入：

```html
<link rel="stylesheet" href="./global-styles.css">
```

然后使用 CSS 变量：

```css
.my-element {
  color: var(--ob-blue-5);
  border-radius: var(--ob-radius-md);
  padding: var(--ob-space-200);
}
```

## 许可证

MIT

## 贡献

欢迎提交 Issue 和 Pull Request！
